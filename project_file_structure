/2026-Pro-01
│
├── /backend (Node.js/Express)
│   ├── /src
│   │   ├── /config             # DB 설정, API Key 관리 (카카오, TourAPI)
│   │   ├── /controllers        # API 요청 처리 (검색, 일정 생성, 저장)
│   │   ├── /models             # DB 테이블 정의 (User, Itinerary, Place)
│   │   ├── /routes             # 엔드포인트 경로 (searchRoute, planRoute)
│   │   ├── /services           # 핵심 로직 (최단 동선 계산, 타임테이블 생성)
│   │   └── /utils              # 공통 함수 (시간 계산, 거리 연산)
│   ├── .env                    # 환경변수 파일
│   ├── app.js                  # 서버 진입점 (포트 개방 및 미들웨어 설정)
│   └── package.json
│
├── /frontend (React or Next.js)
│
└── .gitignore                  # Git 관리 제외 항목